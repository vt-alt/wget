--- wget-1.8.1.orig/src/netrc.c
+++ wget-1.8.1/src/netrc.c
@@ -313,8 +313,11 @@
 	  }
 
 	  /* if field was quoted, squash the trailing quotation mark */
+	  /* and reset quote flag */
-	  if (quote)
+	  if (quote) {
 	    shift_left(p);
+	    quote = 0;
+	  }
 
 	  /* Null-terminate the token, if it isn't already.  */
 	  if (*p)
